#!/bin/bash

# gfalgs
export GFLAG_VERSION=2.2.1
export GFLAG_FULLNAME=gflags-${GFLAG_VERSION}
export GFLAG_TAR_NAME=${GFLAG_FULLNAME}.tar.gz

# glogs
export GLOG_VERSION=0.3.5
export GLOG_FULLNAME=glog-${GLOG_VERSION}
export GLOG_TAR_NAME=${GLOG_FULLNAME}.tar.gz

# googletest
export GOOGLETEST_VERSION=1.8.0
export GOOGLETEST_FULLNAME=googletest-${GOOGLETEST_VERSION}
export GOOGLETEST_TAR_NAME=${GOOGLETEST_FULLNAME}.tar.gz


# protobuf
export PROTOBUF_VERSION=3.4.0
export PROTOBUF_FULLNAME=protobuf-${PROTOBUF_VERSION}
export PROTOBUF_TAR_NAME=${PROTOBUF_FULLNAME}.tar.gz
